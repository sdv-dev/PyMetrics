name: Downloads Collection

on:
  workflow_dispatch:
    inputs:
      project:
        description: 'Name of the spreadsheet to collect'
        required: false
        default: ''
      repositories:
        description: 'List of projects (aka. libraries) to collect, separated by spaces'
        required: false
        default: ''
      output-folder:
        description: 'ID of the Google Drive Output folder'
        required: false
        default: '10QHbqyvptmZX4yhu2Y38YJbVHqINRr0n'
      max-days:
        description: 'Maximum number of days to collect, starting from today.'
        required: false
        default: '7'
      extras:
        description: 'Extra arguments to pass (e.g. --dry-run --force --start-date)'
        required: false
        default: ''
  schedule:
    - cron:  '0 0 * * *'

jobs:
  collect:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v1
      with:
        python-version: 3.8
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        python -m pip install .
    - name: Collect Downloads Data
      run: |
        download-analytics collect -v \
          -s ${{ github.event.inputs.spreadsheet }} \
          -p ${{ github.event.inputs.projects }} \
          -m ${{ github.event.inputs.max-days }} \
          -o gdrive://${{ github.event.inputs.output-folder }} \
          ${{ github.event.inputs.extras }}
      env:
        PYDRIVE_CREDENTIALS: ${{ secrets.PYDRIVE_CREDENTIALS }}
        BIGQUERY_CREDENTIALS: ${{ secrets.BIGQUERY_CREDENTIALS }}
